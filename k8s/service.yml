apiVersion: v1
kind: Service
metadata:
  name: cowallet
  namespace: cowallet
  labels:
    app: cowallet
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: cowallet
